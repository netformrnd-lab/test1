# 이 저장소에서 일할 때 지켜야 할 것

넷폼알앤디 **CRM마케팅팀**의 콘텐츠 마케팅 자동화 저장소입니다.

## 사용자에 대해

- 사용자는 **개발자가 아닌 마케터**입니다. 전문용어 대신 쉬운 말로 안내하세요.
- 명령어 이름·파일 경로는 **그대로 복사할 수 있게** 코드블록으로 보여주세요.
- 한 번에 3개 넘게 질문하지 마세요.

## 구조

| 경로 | 용도 |
|---|---|
| `브랜드기준/` | 팀의 정답지. **모든 콘텐츠 작업 전에 먼저 읽습니다** |
| `.claude/skills/cm-*/` | 실제 자동화 (`/cm-blog`, `/cm-review` 등 9개) |
| `콘텐츠/` | 만들어진 결과물 저장소 |
| `ai-office/` | 픽셀 사무실 시각화 (별도 Next.js 앱) |
| `index.html` | 기존 전사 KPI 대시보드 — **요청 없이 건드리지 마세요** |

## 콘텐츠를 만들거나 검수할 때

1. **항상 `브랜드기준/` 을 먼저 읽습니다.** 원메시지·톤앤매너·페르소나·여정맵·제작기준.
2. `( 여기를 채워주세요 )` 는 빈칸입니다. 비었다고 멈추지 말고 진행하되,
   **"기준이 없어 제가 정한 것"** 을 마지막에 목록으로 보고하세요.
3. 결과물은 `콘텐츠/` 하위에 저장하고, 파일 맨 위에 정보 표(브랜드/여정단계/검수상태)를 답니다.

## 절대 하지 않는 것

- **수치·인증·수상·순위·가격·납기를 지어내는 것.**
  모르면 `[확인 필요: 항목명]` 으로 비워두고 사용자에게 알립니다.
  (검색량, 전환율, 이탈률도 마찬가지입니다. 확인 안 했으면 `미확인` 이라고 씁니다.)
- **사용자를 대신해 발행·배포·발송하는 것.** 초안까지가 AI의 몫입니다.
- **VOC 원문을 다듬는 것.** 고객이 쓴 말 그대로가 자산입니다. 단, 개인정보는 마스킹합니다.
- **기존 `브랜드기준/` 내용을 확인 없이 덮어쓰는 것.** VOC는 쌓기만 하고 지우지 않습니다.
- 확인되지 않은 것을 확인된 것처럼 말하는 것.

## ai-office/ 를 만질 때

`ai-office/CLAUDE.md` 의 규칙을 따르세요. 요약하면:
- **`company.config.ts` 한 파일만** 고칩니다.
- 부서 `id` 를 바꾸지 않습니다. 부서는 **12개 유지**.
- `app/game/` 안의 엔진 파일은 건드리지 않습니다.
- 화면 하단 제작자(갓생맘 🎀) 크레딧은 지우지 않습니다.
- `.dev.vars` 는 비밀번호와 같습니다. 출력·커밋 금지.

## 팀 운영 원칙 (콘텐츠에 반영되어야 함)

- **설계는 3명이 함께 확정합니다.** 페르소나·여정·원메시지·콘텐츠 캘린더.
- **채널 담당자는 확정된 설계를 자기 채널 포맷으로 옮길 뿐, 메시지를 새로 만들지 않습니다.**
- 발행 전 `/cm-review` 는 건너뛰지 않습니다.
