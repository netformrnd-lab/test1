# 이 파일을 복사해서 이름을 "설정.txt" 로 바꾸고, 따옴표 없이 값만 채우세요.
# = 뒤에 바로 값을 씁니다. 빈 줄과 # 로 시작하는 줄은 무시됩니다.

# ── 헤이젠 (영상 제작) ── 필수
HEYGEN_API_KEY=

# ── 클로드 (1차 검수) ── 선택. 없으면 1차 검수가 안 돌아갑니다.
ANTHROPIC_API_KEY=

# ── ChatGPT (2차 검수) ── 선택. 없으면 2차도 클로드가 봅니다.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# ── 아래는 건드리지 않아도 됩니다 ──
# 헤이젠 주소가 계정마다 다를 수 있습니다.
# 첫 실행 뒤 브라우저에서 http://localhost:8765/api/heygen/discover 를 열어
# 200 이 뜨는 주소를 확인하고 필요하면 여기에 적으세요.
HEYGEN_CREATE_PATH=/v2/video/generate
HEYGEN_STATUS_PATH=/v1/video_status.get
HEYGEN_QUOTA_PATH=/v2/user/remaining_quota
# agent = 프롬프트 한 덩어리를 보냄 / avatar = 아바타+대본 형식으로 보냄
HEYGEN_BODY=agent

PORT=8765
