# studio/data

`reference.json` 은 **여기에 올리지 않습니다.** 이 저장소는 공개이고,
그 파일에는 제안서에서 옮긴 사내 하자 사전이 들어 있습니다.
실제 값은 제작실 아티팩트(비공개) 안에만 있습니다.

담고 있는 것:

| 키 | 크기 | 무엇 |
|---|---|---|
| `회사` | dict(3) | |
| `대상` | list(4) | |
| `공종` | list(3) | |
| `주제후보` | list(24) | |
| `하자` | list(53) | |
| `현상` | dict(8) | |
| `물리공통` | dict(6) | |
| `카메라` | dict(4) | |
| `조명` | dict(6) | |
| `그리기금지` | list(13) | |
| `실제크기` | dict(8) | |

`store.json` 은 페이지가 스스로 다시 발행하는 데이터 파일입니다
(영상 목록·설정·지식 메모). 여기 있는 것은 빈 초기값입니다.
